specVersion: "v1beta"
serviceId: "planner"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "planner-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
